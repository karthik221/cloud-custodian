
policies:
  - name: filter-tag-aws-account
    resource: aws.account 
    description: "Tag AWS account with Environment=Production"
    filters:
      - "tag:Environment": absent
    actions:
      - type: tag
        key: Environment
        value: Production
